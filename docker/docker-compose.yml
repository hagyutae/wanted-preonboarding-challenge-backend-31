version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: my_postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: commerce
      POSTGRES_USER: wanted
      POSTGRES_PASSWORD: wanted123!@#
